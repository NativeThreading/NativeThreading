modLoader = "javafml"
loaderVersion = "[4,)"
license = "MIT"

[[mods]]
modId = "palette"
version = "${version}"
displayName = "Palette Compression"
description = "Optimized palette container with 4-tier adaptive storage for chunk sections."
authors = ["uright008"]

[[mixins]]
config = "palette.mixins.json"
