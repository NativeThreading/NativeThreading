modLoader = "javafml"
loaderVersion = "[4,)"
license = "MIT"

[[mods]]
modId = "parallel-core"
version = "${version}"
displayName = "Parallel Core"
description = "Central thread pool, world management, and /parallel command system for NativeThreading mods."
authors = ["uright008"]

[[mixins]]
config = "parallel-core.mixins.json"
